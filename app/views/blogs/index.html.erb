<h1>Listes de blogs</h1>

<table>
  <tr>
    <th>Titre</th>
    <th>Content</th>
  </tr>

<% @blogs.each do |blog| %>
  <tr>
    <td><%= blog.title %></td>
    <td><%= blog.content %></td>
    <td><%= link_to 'Vérifiez les détails', blog_path(blog.id) %></td>
  </tr>
<% end %>
</table>

<%= link_to 'publier un nouveau blog', new_blog_path %>